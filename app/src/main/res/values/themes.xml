<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Roadguard draws its entire UI with Compose and Material 3, so the platform theme only
      has to provide a sane pre-Compose window: no action bar, a dark window background so
      launching into the driving UI does not flash, and transparent system bars because the
      app draws edge to edge.
    -->
    <style name="Theme.Roadguard" parent="android:Theme.DeviceDefault.DayNight">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/roadguard_window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
